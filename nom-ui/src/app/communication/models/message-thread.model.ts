import { BaseCommonModel } from '../../common/models/_base-common.model';

export interface MessageThreadModel extends BaseCommonModel {
    // Properties for displaying thread info, e.g., participants, last message snippet
}